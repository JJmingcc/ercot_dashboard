# Runtime dependencies for the Streamlit dashboard.
# This is the file Streamlit Community Cloud installs, so it must include the UI deps
# (streamlit / plotly / shapely), not just the data-pipeline ones.
python-dotenv>=1.0
requests>=2.31
duckdb>=1.0
pandas>=2.0
numpy>=1.26
pyarrow>=15.0
streamlit>=1.40
plotly>=5.24
shapely>=2.0
