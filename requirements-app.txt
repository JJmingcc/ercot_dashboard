# App (Streamlit dashboard) dependencies.
# Everything now lives in requirements.txt (so Streamlit Cloud, which installs that file,
# gets the UI deps too); this is kept as a stable entry point for run.sh / docs.
-r requirements.txt
